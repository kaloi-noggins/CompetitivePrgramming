#include <bits/stdc++.h>

using namespace std;

int main()
{
    int l, c;
    cin >> l;
    cin >> c;

    char mapa[l][c];

    for (int i = 0; i < l; i++)
    {
        for (int j = 0; j < c; j++)
        {
            cin >> mapa[i][j];
        }
    } 

    for (int i = 0; i < l; i++)
    {
        for (int j = 0; j < c; j++)
        {
            cin >> mapa[i][j];
        }
    }

    return 0;
}
